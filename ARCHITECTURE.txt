```
┌─────────────────────────────────────────────────────────────────────┐
│                     vLLM REQUEST FLOW (THROTTLED)                   │
└─────────────────────────────────────────────────────────────────────┘

                      ┌─────────────────┐
                      │  Worker wants   │
                      │  to call vLLM   │
                      └────────┬────────┘
                               │
                               ▼
                    ┌──────────────────────┐
                    │  Circuit Breaker OK? │
                    └──────┬───────┬───────┘
                          YES     NO
                           │       └──────────► Reject immediately
                           │                    (server overloaded)
                           ▼
                ┌────────────────────────┐
                │  Acquire Rate Limiter  │
                │  Token (wait if none)  │
                └──────────┬─────────────┘
                           │
                           │  ⏳ May wait here if rate limit hit
                           │     (20 requests/min = ~3s avg gap)
                           │
                           ▼
              ┌──────────────────────────────┐
              │  Acquire Concurrency Slot    │
              │  (max 2 concurrent)          │
              └────────────┬─────────────────┘
                           │
                           │  ⏳ May wait here if 2 requests in-flight
                           │
                           ▼
                  ┌─────────────────┐
                  │  Make LLM Call  │
                  └────┬──────┬─────┘
                      OK    ERROR
                       │      │
                       │      ▼
                       │   ┌──────────────────┐
                       │   │  Is Retryable?   │
                       │   │  (timeout, 503,  │
                       │   │   rate limit)    │
                       │   └────┬───────┬─────┘
                       │       YES     NO
                       │        │       │
                       │        │       └──► Record Failure
                       │        │            Open Circuit Breaker?
                       │        │            (if 5 consecutive fails)
                       │        │
                       │        ▼
                       │   ┌──────────────────────┐
                       │   │  Calculate Backoff   │
                       │   │  with Jitter         │
                       │   │                      │
                       │   │  Attempt 1: ~1s      │
                       │   │  Attempt 2: ~2s      │
                       │   │  Attempt 3: ~4s      │
                       │   │  Attempt 4: ~8s      │
                       │   │  Attempt 5: ~16s     │
                       │   └──────────┬───────────┘
                       │              │
                       │              ▼
                       │         ⏳ Sleep
                       │              │
                       │              └──► Retry (back to "Make LLM Call")
                       │
                       ▼
              ┌─────────────────┐
              │  Record Success │
              │  Reset Failures │
              └────────┬────────┘
                       │
                       ▼
              ┌─────────────────┐
              │ Release Rate    │
              │ Limiter & Slot  │
              └─────────────────┘


┌─────────────────────────────────────────────────────────────────────┐
│                    CONCURRENCY VISUALIZATION                        │
└─────────────────────────────────────────────────────────────────────┘

WITHOUT throttling (old):
Workers: [W1] [W2] [W3] [W4] × 10 consumers each = 40 consumers
         |||||||||| |||||||||| |||||||||| ||||||||||
         ↓↓↓↓↓↓↓↓↓↓ ↓↓↓↓↓↓↓↓↓↓ ↓↓↓↓↓↓↓↓↓↓ ↓↓↓↓↓↓↓↓↓↓
         ══════════════════════════════════════════
                        vLLM Server
         💥 OVERLOAD! GPU OOM! Crashes! 💥


WITH throttling (new):
Workers: [W1] [W2] [W3] [W4] × 3 consumers each = 12 consumers
         |||    |||    |||    |||
         ↓↓↓    ↓↓↓    ↓↓↓    ↓↓↓
         ════════════════════════
         ⏳ ⏳ ⏳ ⏳ ⏳ ⏳ ⏳ ⏳ ⏳ ⏳  ← Most waiting in queue
         ══════════════════════════
         Max 2 concurrent ──► [█] [█] ← vLLM Server
         Max 20/min       ──►  ✅ Happy GPU!


┌─────────────────────────────────────────────────────────────────────┐
│                     BACKOFF TIMING EXAMPLE                          │
└─────────────────────────────────────────────────────────────────────┘

Time: 0s
  │
  └─► Attempt 1: Call vLLM
         ❌ Timeout! (server busy)
         
Time: ~1s (1s + jitter)
  │
  └─► Attempt 2: Call vLLM
         ❌ Connection error!
         
Time: ~3s (wait 2s + jitter)
  │
  └─► Attempt 3: Call vLLM
         ❌ Connection error!
         
Time: ~7s (wait 4s + jitter)
  │
  └─► Attempt 4: Call vLLM
         ❌ Connection error!
         
Time: ~15s (wait 8s + jitter)
  │
  └─► Attempt 5: Call vLLM
         ✅ Success!
         
Total elapsed: ~15 seconds (much better than crashing!)


┌─────────────────────────────────────────────────────────────────────┐
│                   CIRCUIT BREAKER STATES                            │
└─────────────────────────────────────────────────────────────────────┘

CLOSED (normal):
  ┌─────────────┐
  │ All requests│
  │   allowed   │
  └─────────────┘
        │
        ├──► ✅ Success ──► Continue
        │
        └──► ❌ Fail ──► Increment counter
                           (5 fails → OPEN)

OPEN (protecting):
  ┌─────────────┐
  │ All requests│
  │   BLOCKED   │  ← Prevents cascading failures
  └─────────────┘
        │
        └──► Wait 60 seconds ──► HALF-OPEN

HALF-OPEN (testing):
  ┌─────────────┐
  │ Test request│
  │   allowed   │
  └─────────────┘
        │
        ├──► ✅ Success ──► CLOSED (recovered!)
        │
        └──► ❌ Fail ──► OPEN (still broken)


┌─────────────────────────────────────────────────────────────────────┐
│                      KEY BENEFITS                                   │
└─────────────────────────────────────────────────────────────────────┘

✅ NO GPU OVERLOAD       Max 2 concurrent requests at any time
✅ SMOOTH THROTTLING     20 requests/min = steady 3s gap average  
✅ SMART RETRY           Exponential backoff handles transient issues
✅ PREVENTS STAMPEDES    Jitter prevents synchronized retries
✅ GRACEFUL DEGRADATION  Circuit breaker stops cascade failures
✅ SELF-HEALING          Auto-recovery when server comes back
✅ NO LOST TASKS         RabbitMQ requeues failed messages

🐘 NO CASCADING ELEPHANTS! 🐘
```
